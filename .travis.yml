sudo: required
dist: trusty

language: node_js
node_js:
  - '10'

cache:
  directories:
    - node_modules

install:
  - yarn install --frozen-lockfile --non-interactive

script:
  - yarn lint
  - yarn test:ci
  - yarn build:app
  - yarn build:lib
